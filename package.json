{
  "name": "udep-aplicacion-movil",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-frontend": "cd frontend && npm install",
    "build": "cd frontend && npm install && npm run build:web && cd .. && node copy-dist.js",
    "start": "echo 'App is hosted on Vercel'"
  }
}
